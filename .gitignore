node_modules

# Build output
.next/
out/
*.tsbuildinfo

# OS junk
.DS_Store
**/.DS_Store

# Local Claude Code tooling config (dev-server launch config, local
# permission settings) — machine-specific, not shared project config.
.claude/

# Environment files — never commit real secrets/config.
# .env.example is intentionally NOT ignored; it should stay in version control
# as a template.
.env
.env.local
.env*.local
