/**
 * @deprecated Import from "@/lib/analytics/events" instead. Kept as a
 * re-export so existing imports of `trackPhoneCallClick` from this path
 * continue to work unchanged.
 */
export { trackPhoneCallClick } from "./events";
